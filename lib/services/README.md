# Services

Pure-logic utility libraries extracted from managers for testability.
No hardware dependencies. Run on host via `pio test -e native_test`.

See [CLAUDE.md](CLAUDE.md) for the full service reference (API, dependency graph, test coverage).
