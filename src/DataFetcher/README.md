# DataFetcher

External HTTP data source polling. Periodically fetches JSON values from APIs and pushes them as custom apps on the display.

See [CLAUDE.md](CLAUDE.md) for the full AI reference (DataSourceConfig, fetch flow, REST endpoints, persistence).
