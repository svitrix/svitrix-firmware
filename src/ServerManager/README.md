# ServerManager

HTTP REST API server, WiFi connectivity, mDNS/UDP discovery, and TCP server. Primary external control interface alongside MQTT.

See [CLAUDE.md](CLAUDE.md) for the full AI reference (endpoint table, communication protocols, AP fallback).
