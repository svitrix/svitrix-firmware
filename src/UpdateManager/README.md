# UpdateManager

OTA firmware update manager. Checks a remote server for new firmware versions
and performs HTTPS downloads with a progress bar on the LED matrix.

See [CLAUDE.md](CLAUDE.md) for the full AI reference (files, OTA flow, interfaces, config fields).
